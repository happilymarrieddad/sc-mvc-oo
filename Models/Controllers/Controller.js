"use strict"
class Controller {
	constructor() {
		
	}

	index(req,callback) {
		// The body is the html body from express

		callback(null,{})
	}

	create(req,callback) {
		// The body is the html body from express

		callback(null,{})
	}

	store(req,callback) {
		// The body is the html body from express

		callback(null,{})
	}

	update(req,id,callback) {
		// The body is the html body from express

		callback(null,{})
	}

	delete(req,id,callback) {
		// The body is the html body from express

		callback(null,{})
	}

	show(req,id,callback) {
		// The body is the html body from express

		callback(null,{})
	}

	edit(req,id,callback) {
		// The body is the html body from express

		callback(null,{})
	}

}

module.exports = Controller

module.exports.create = new Controller()